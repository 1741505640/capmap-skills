---
description: 文档方案主状态禁止跳步（尤其未验证不得已落地）
alwaysApply: true
---

# 文档状态禁止跳步

改 `<docs_root>` 下方案 / 测试 Tag 时：

```text
方案中 → 已确认 → 开发中 → 已开发 → 验证中 → 已验证 → 落地中 → 已落地 → 已归档
```

**硬约束（不可跳）：**

1. 未到 **`已验证`**（测试通过，或测试文明示免测并标已验证）之前，禁止标 **`落地中` / `已落地`**。
2. 对应测试文仍为 **`测试中`** 时，方案禁止落地态。
3. `capmap-dev` 最多标到 **`已开发`**，禁止顺手已落地。
4. 真相与细则：`.agents/skills/capmap-system/reference/status-tags.md`；违规由 `capmap-lint` 报 `status_skip`。
