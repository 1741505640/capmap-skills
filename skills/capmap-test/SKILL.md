---
name: capmap-test
description: >-
  功能测试文档：测试范围、改动影响面与回归点、产物与人工验收步骤、测试是否通过的记录；
  位于 <docs_root>/测试/<主题>/。在用户确认转测试（或点名写测试）后执行；
  先写计划与用例，结果列可未测。适用于「写测试点」「回归范围」「记一下测过了」、
  「转测试」。可独立使用。
---

# capmap-test — 测试计划与记录

原则：[lifecycle](../capmap-system/reference/lifecycle.md) · 模板：[templates](../capmap-system/reference/templates.md) · Tag：[status-tags](../capmap-system/reference/status-tags.md)

## 启动

1. 读 `.agents/skills/capmap-system/capmap.yaml` → `docs_root`
2. 确认主题、能力名、相关方案/底图
3. 方案宜已是 `已开发`（由 [capmap-dev](../capmap-dev/SKILL.md) 收尾后用户同意转测，或用户直接点名本 Skill）

## 路径与命名

```text
<docs_root>/测试/<主题>/测试-<功能可读名>.md
```

例：`测试/权限-abac/测试-三级目录权限.md`  
图谱靠文件名区分，禁止 `README.md`。

## 两拍节奏（降卡顿）

| 拍 | 谁做 | 文档/Tag | 不要做 |
|----|------|----------|--------|
| **拍 1 · 交测试文** | Agent（用户确认转测后） | 写满范围/影响面/用例步骤与期望；结果列默认 **未测**；测试文 `测试中`；方案 `验证中` | 不要等「测过再说」才写文；不要空等用户回「测完了」才开工 |
| **拍 2 · 记结论** | 人测后告知，或用户明示免测 | 填结果；通过/免测 → 测试文与方案 `已验证` | Agent **禁止**无证据自评通过 |

入口：`capmap-dev` 标完已开发后会**询问**是否转测；用户同意（或本回合已点名）再执行拍 1。  
拍 1 结束后回复可附 **3～7 条建议自测要点**，然后停住等人填结果——**文档与 Tag 已就位**。

## 文档必备节

1. **背景**：对应能力 / 方案 / 底图链接  
2. **测试范围**：要测什么（功能点列表）  
3. **影响面与回归**：改动可能波及哪里、建议回归哪些路径  
4. **产物与人工验收**：产物清单（路径 + 生成命令）、每条判据"应该看到什么"、反例含义  
5. **用例与记录**：步骤 / 期望 / 结果（通过/失败/阻塞/**未测**）/ 日期 / 执行人  
6. **结论**：是否可发布；遗留问题；免测说明（若有）  

首次落地拍 1 时：1～5 写全，结果一律「未测」，结论写「待人测」。

## 产物与人工验收（**写在测试文档里**，拍 1 必写）

> **数字证明我没算错，证明不了人看到的效果。** 人能看出来/听出来的判据，
> 必须留下**能直接打开**的产物，否则用户只能反复问「结果在哪、我怎么目检」。

| 要写清 | 例 |
|--------|-----|
| 产物路径 | `D:\...\.e2e-out\06\longvideo_transition_clip.mp4`（写死，可直接打开） |
| 生成命令 | `python scripts/demo_06_evidence.py`（脚本化，可重跑） |
| 应该看到/听到什么 | 「两页**可见地叠化**，不是一帧换页」 |
| 反例含义 | 「仍是硬切 → 改法没生效」「有黑场 → 参数要调」 |

纪律：

- 效果类判据（视觉/听觉/交互）**禁止只给数字**；帧数/SSIM/LUFS 是辅证，不是产物
- 反差类判据给**对照文件**（修复前 vs 修复后并排），或把素材本身一并给出
- 让差异**自己说话**：能用带计时器/标注的素材，就别用纯色块
- 产物**可重复生成**且按主题/切片**分目录**；生成脚本**只清自己那个目录**，
  **禁止** `rmtree` 共享产物根（会连带删掉其它主题的产物）
- 被 gitignore 的产物目录，要写清**怎么重新生成**

> **有切片时**，同一套产物与验收方式**也要写进切片文档**（见 `capmap-gate` 交票规则），
> 两边保持一致 —— 切片验收与功能测试用的是同一批产物。

## Checklist

```
- [ ] 1. 解析 docs_root；创建或更新 测试-<功能>.md
- [ ] 2. 测试文：`测试` + `状态/测试中`；对应方案标 `验证中`（拍 1）
- [ ] 3. 写清范围 + 影响面/回归 + 可执行用例；结果可先「未测」
- [ ] 4. **产物与人工验收**：产物路径 + 生成命令 + 每条判据"应该看到什么" + 反例含义
- [ ] 5. 拍 2 通过或免测：测试文 → `已验证`；方案 → `已验证`；底图 §1 可写已验证（无 Tag）  
  **未通过前禁止**把方案标成 `落地中` / `已落地`
- [ ] 6. 失败：测试文保持 `测试中`；方案保持 `验证中`
```

## 与开发的关系

- 通常在 [capmap-dev](../capmap-dev/SKILL.md) 标完 `已开发` **并经用户确认转测**（或用户直接点名本 Skill）后执行拍 1  
- 用户说「免测/仅冒烟」→ **必须**在测试文档结论写明免测范围与日期，测试文标 `已验证` 后，方案才可 `已验证`；**禁止**无测试文直接已落地  

## 硬约束

- 测试文仍为 `测试中` ⇒ 方案不得 `落地中`/`已落地`（见 [status-tags](../capmap-system/reference/status-tags.md)）  
- Agent 不得把冒烟自述直接写成「全部通过」除非用户确认或明示免测  

## 不做

- 不代替自动化测试框架配置  
- 不把长期调用手册写进测试文档 → [capmap-norm](../capmap-norm/SKILL.md)  
- 不因「还没测」回退方案的 `已开发`  
- 不在用户未确认转测时擅自从 dev 静默开写测试文（用户已点名除外）  
